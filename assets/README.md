# Assets

Add your background music file here as `music.mp3` (referenced by `#bgMusic` in `index.html`).

Use a royalty-free or licensed track — for example from:
- YouTube Audio Library
- Pixabay Music
- Bensound

Keep the file reasonably small (under ~5MB) so the page loads quickly.
